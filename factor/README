This program takes a LIST of numbers, separated by commas, and finds
all factors of the numbers in the list with a runtime of
O(sqrt(n)). This program also tells the user if the inputs are
abundant numbers.
